import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  flexContainer: {
    flex: 1,
  },
  fillParent: {
    width: '100%',
    height: '100%',
  },
  flexCenter: {
    justifyContent: 'center',
    alignItems: 'center',
  }
});
